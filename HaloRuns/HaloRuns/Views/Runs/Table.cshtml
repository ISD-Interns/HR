@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
        public int Date { get; set; }
        [DisplayName("time")]
        public int Time { get; set; }
        [DisplayName("edition")]
        public int EditionId { get; set; }
        public int Id { get; set; }
        [DisplayName("difficulty")]
        public int DifficultyId { get; set; }
        public string Url { get; set; }
        public int MapId { get; set; }
        public map Map { get; set; }
        public int UserId { get; set; }
*@

@section title{Runs}

@model HaloRuns.Models.Run
<button type ="button" id="runbutton">
    Click
</button>

<table style="width:100%">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(r => r.Id)
            </th>
            <th>
                @Html.DisplayNameFor(r => r.Date)
            </th>
            <th>
                @Html.DisplayNameFor(r => r.Time)
            </th>
            <th>
                @Html.DisplayNameFor(r => r.Edition.Name)
            </th>
        </tr>
    </thead>
</table>

@section Scripts{ 
    <script>
        $(document).ready(function () {
            $.get('@(Url.Action("dataTableParam", "Runs"))', null, function (data) {
                var table = $('table').DataTable(data);
            })
        })
        /*function buttonclickcallback() {
            console.log("button has been clicked");
        }
        function registerButtonEventHandler() {
            $('#runbutton').mouseup(buttonclickcallback);
        }
        */
        @*
        $(document).ready(function () {
            var table = $('table').DataTable({
                "searching" : false,
                "bPaginate": true,
                "bLengthChange": false,
                "bFilter": true,
                "processing": true,
                "serverSide": true,
                "ajax": {
                    "url": "@Url.Action("DataTablesCallback", "Runs")",
                    "type": "POST",
                    "datatype": "json",
                },
                "bInfo": false,
                "bAutoWidth": true,
                "order": [[1, "asc"]],
            });
        });
        *@
        $(document).ready(function ()
        {
            $('#runbutton').mouseup(function () {
                console.log("button has been clicked");
            });
        });
        //$(document).ready(initalizeDataTables);
        //$(document).ready(registerButtonEventHandler);
    </script>
} 

